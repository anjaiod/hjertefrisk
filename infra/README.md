# Infrastructure

This folder contains infrastructure scaffolding for `hjertefrisk`.

Current layout:

- `terraform/gcp`: GCP networking and compute scaffold

Keep environment values outside Terraform source code by using `env/*.tfvars`.
